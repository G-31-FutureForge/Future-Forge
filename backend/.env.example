# Example environment variables for backend
# YouTube Data API v3 key (required for YouTube results)
YOUTUBE_API_KEY=your_youtube_api_key_here

# Optional: override Coursera API base
# COURSERA_API_BASE=https://api.coursera.org/api/courses.v1

# Other env vars used by the project (DB, JWT, etc.) are unchanged.
